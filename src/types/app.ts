export type DifficultyLevel = "beginner" | "intermediate" | "advanced";

export type FeatureStatus = "planned" | "scaffolded" | "active";

export type NavItem = {
  label: string;
  href: string;
  description?: string;
};
